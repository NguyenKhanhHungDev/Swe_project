{
  "name": "brewlite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --names BACKEND,FRONTEND \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev -- --port 3000\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  }
}